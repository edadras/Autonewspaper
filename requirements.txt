# ---------------------------------------------------------------------------
# AI Newspaper Studio - runtime requirements
# ---------------------------------------------------------------------------
PySide6>=6.6
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
Pillow>=10.2
python-docx>=1.1
pypdf>=4.0
beautifulsoup4>=4.12
lxml>=5.1

# --- Windows only: Adobe COM / UI automation -------------------------------
pywin32>=306; sys_platform == "win32"
comtypes>=1.4; sys_platform == "win32"
pywinauto>=0.6.8; sys_platform == "win32"
keyring>=25.0; sys_platform == "win32"

# --- Optional fallback layer (mouse/keyboard, last resort) -----------------
pyautogui>=0.9.54; sys_platform == "win32"
